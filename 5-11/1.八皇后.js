/**
 * @param {number} n
 * @return {string[][]}
 */
var solveNQueens = function (n) {
    //创建一个二维数组
    const board = new Array(n).fill('.').map(x => new Array(n).fill('.'));
    console.log(board);

   let cols = new Set();
   let line1 = new Set();
   let line2 = new Swt();

   //一行一行的找  不能在

    function bfs() {
        
    }
};
solveNQueens(4)